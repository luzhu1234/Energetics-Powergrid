# Energetics Power Grid 更新日志

## 1.1.0

### 新增
- **逆变器（Inverter）**：复用双闸刀开关的模型与形状。
  - 四个端子：1、2 为直流输入，3、4 为交流输出，接线时悬停可看到端子标注。
  - 闸刀打开（默认）：输入与输出直通（接触电阻极低），直流直接通过。
  - 闸刀闭合：逆变模式，输出端输出固定 50Hz 方波交流电（频率可在配置中调整），输出电压幅值跟随输入电压。
  - 不凭空发电：输出带多少负载，输入就抽取对应的功率；空载时输入呈高阻抗，几乎不耗电。
  - 性能：方波通过电力学的 micro tick（微刻）机制在求解线程内求值，主线程每个方块每刻只有几次算术运算。
  - 配置项：`inverterResistance`（直通接触电阻）、`inverterOutputResistance`（输出内阻）、`inverterFrequency`（输出频率，默认 50Hz）。
  - 合成：双闸刀开关 + 4 红石（无序合成）。

### 修复
- 逆变器初次构建时缺失方块状态与物品模型资源，导致方块无材质无模型。
- **逆变器接入设备后电压飙升烧毁设备**：输出源此前被求解器按理想电压源处理（无内阻、电流无上限，矩阵非正定可能发散），且输入侧负载电阻的反馈是恒等循环、从不跟随实际输出功率。现改为与三相碳刷一致的 Norton 等效形式（电流受限 v/R），输出功率在求解器内累计，输入侧按实际输出功率抽取——能量守恒，空载几乎不耗电。

## 1.0.2

### 修复
- **励磁定子接线点位置**：接线端子（正/负极）与模型磁极面相反，已镜像到与模型开口面一致，所有朝向下跟随模型翻转。
- **转子假应力过载**：重进存档后转子一直显示应力过载、必须拆掉定子重放的问题。根因是客户端缺区块时会用错误数据重建动力学网络视图，且服务端从不同步修正；现在客户端完全使用服务端同步的数值，不再本地扫描方块。
- **转子 mixin 客户端漏洞**：客户端未正确接管原版 lazyTick，导致客户端用只认原版定子的扫描覆盖有效磁铁数。

## 1.0.1

### 新增
- **励磁定子（Excitation Stator）**：复用电力学定子的模型与材质。
  - 两个接线端子位于模型开口面，1 号为正端子、2 号为负端子（护目镜可见标注）。
  - 同一直线、同一朝向的励磁定子自动并联（正连正、负连负），无需右键连接。
  - 励磁强度 B = I × U × y（y 默认 1，可配置），励磁内阻 100Ω（可配置）；励磁强度像风扇转速一样缓慢爬升，通入交流电会自行抵消。
  - 戴上工程师护目镜可查看实时励磁强度。
- **转子逻辑扩展**：
  - 取转子周围励磁定子励磁强度之和的绝对值作为应力消耗依据，1000 励磁强度等同于一个原版定子；
  - 原版定子对转子的应力消耗削弱为十分之一；
  - 发电量随有效磁铁数同步削弱，应力与发电保持一致；
  - 三相交流发电机碳刷：负载功率增大时输出频率短暂下坠（不超过 0.08Hz，不为负），约 5 秒内逐渐恢复；负载减小时同理上浮。
- **放置体验**：准星对准转子右键即可像原版定子一样自动放置励磁定子（自动对准朝向）。

### 修复
- 编译目标与运行时不一致导致的 `NoSuchMethodError` 崩溃（编译目标切换到 1.2.0）。
- 励磁爬升速度过慢导致"看起来不起作用"，改为与风扇一致的速度。
- Create 网络重载后应力记账与实际削弱值不一致的问题。

## 1.0.0

- 首个版本：将交错电网的灯座、灯泡、催熟灯、工厂灯、加热线圈、换向开关等移植到电力学体系。
